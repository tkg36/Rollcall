AWSTemplateFormatVersion: '2010-09-09'
Description: S3 buckets for the RollCall ETL pipeline

Parameters:
  CsvBucket:
    Type: String
    Description: S3 bucket for processed XLSX attachments (csvParser writes, rollcall-lambda reads)
  DeptsBucket:
    Type: String
    Description: S3 bucket for reference data and output workbook
  ArtifactsBucket:
    Type: String
    Description: S3 bucket for deployment-time artifacts (Lambda layer zips, etc.) — not used at runtime
  SesBucket:
    Type: String
    Description: S3 bucket where SES delivers raw inbound emails

Resources:

  # Receives processed XLSX attachments from csvParser; read by rollcall-lambda
  RollCallCSVBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !Ref CsvBucket
      VersioningConfiguration:
        Status: Enabled
      LifecycleConfiguration:
        Rules:
          - Id: rollcall-auto-delete-30-days
            Status: Enabled
            ExpirationInDays: 30
          - Id: lifecycletesting1
            Status: Enabled
            Filter:
              Prefix: "lifecycletesting/ES&F_GR&S Unfilled Requisition Report 2026-04-27 06_35 EDT-20260429-235207.xlsx"
            ExpirationInDays: 1

  # Holds reference CSVs and output XLSX; S3 event on .xlsx triggers ses-emailer-function
  RollCallDeptsBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !Ref DeptsBucket
      NotificationConfiguration:
        LambdaConfigurations:
          - Id: doneProcessing
            Event: s3:ObjectCreated:Put
            Filter:
              S3Key:
                Rules:
                  - Name: suffix
                    Value: xlsx
            Function: !Sub arn:aws:lambda:${AWS::Region}:${AWS::AccountId}:function:ses-emailer-function

  # Raw inbound email storage; SES delivers here, csvParser reads from here
  RollCallSESBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !Ref SesBucket

  # Deployment-time artifacts only — Lambda layer zips uploaded here before layer stacks deploy
  RollCallArtifactsBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !Ref ArtifactsBucket

Outputs:
  CSVBucketName:
    Value: !Ref RollCallCSVBucket
    Export:
      Name: !Ref CsvBucket

  DeptsBucketName:
    Value: !Ref RollCallDeptsBucket
    Export:
      Name: !Ref DeptsBucket

  SESBucketName:
    Value: !Ref RollCallSESBucket
    Export:
      Name: !Ref SesBucket

  ArtifactsBucketName:
    Value: !Ref RollCallArtifactsBucket
    Export:
      Name: !Ref ArtifactsBucket
