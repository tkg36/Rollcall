# RollCall Pipeline — User Settings
# Edit these values before deploying to a new environment.
# Parameter names here match the CloudFormation parameters in each lambda template.

# ─── Storage ──────────────────────────────────────────────────────────────────

# S3 bucket that receives and stores processed XLSX attachments from inbound emails.
# csvParser writes here; rollcall-lambda reads from here.
CsvBucket: rollcall-s3-csv

# S3 bucket for reference lookup files (cc_id.csv, depts.csv, status.csv, ESF WF data file ref.xlsx)
# and where the completed output workbook is written for each run.
DeptsBucket: rollcall-s3-dev-depts-reference

# S3 bucket used exclusively for deployment-time artifacts (Lambda layer zips, etc.).
# Not used by the pipeline at runtime — only touched during CI/CD deploys.
ArtifactsBucket: rollcall-deployment-artifacts

# S3 bucket where SES delivers raw inbound emails; csvParser reads from here.
SesBucket: rollcall-s3-dev-ses

# ─── VPC (required for rollcall-lambda → RDS connectivity) ───────────────────

# Subnets that have routing to the RDS instance/proxy.
VpcSubnetIds:
  - FILL_SUBNET_ID_1_HERE
  - FILL_SUBNET_ID_2_HERE

# Security groups to attach to rollcall-lambda.
VpcSecurityGroupIds:
  - FILL_SECURITY_GROUP_ID_1_HERE

# ─── Email ────────────────────────────────────────────────────────────────────

# Domain that receives inbound trigger emails and sends outbound result emails.
# Must be verified as an SES domain identity in the target AWS account.
SenderDomain: vanguard.com

# The "From" address used when emailing the output workbook back to the requester.
# Must be an address within SenderDomain and a verified SES sender identity.
SenderEmail: "[Your monitored account]@vanguard.com"
